import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  wrapper: {
    flex: 1,
  },
  themeContainer: {
    flexDirection: 'row',
    marginTop: 16,
  },
  themeName: {
    marginLeft: 12,
  },
})
